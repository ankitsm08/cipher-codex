#include "template.h"

using namespace std;

string temp::encrypt(const string& text) {
  string code = "";

  return code;
}

string temp::decrypt(const string& code) {
  string text = "";

  return text;
}